import React from 'react';

import { ComponentWrap } from '../../wrapper';
import './Header.scss';

const Header = () => {
  return (
    <div>Header</div>
  )
}

export default ComponentWrap(Header, 'home');